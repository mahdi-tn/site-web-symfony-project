<form method="post" action="{{ path('app_products_delete', {'p_ref': product.pRef}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.pRef) }}">
	<button class="btn btn-danger">Delete</button>
</form>
